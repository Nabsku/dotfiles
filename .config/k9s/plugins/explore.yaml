plugins:
  explore:
    shortCut: Shift-E
    description: Explore
    scopes:
      - crd
      - crds
      - customresourcedefinitions
    command: kubectl
    background: false
    confirm: true
    args:
      - explore
      - --context
      - $CONTEXT
      - --api-version
      - $COL-GROUP/$COL-VERSIONS
      - $COL-NAME
