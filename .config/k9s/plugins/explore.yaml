plugins:
  explore:
    shortCut: Shift-E
    description: Explore
    scopes:
      - crd
      - crds
      - customresourcedefinitions
    command: kubectl
    background: false
    args:
      - explore
      - -n
      - $NAMESPACE
      - --context
      - $CONTEXT
      - $COL-KIND
